interface:
  display_name: "Sakura Auth Site Setup"
  short_description: "Build Sakura sites with Japanese auth"
  default_prompt: "Use $sakura-auth-site-setup to add Japanese auth, registration verification, and cron alerts, and require a confirmed Sakura sender mailbox before calling mail delivery complete."
