# 项目维护规则

## Cron 合并与精确调度

以下通用规则用于本仓库涉及的 cron 方案、示例及维护工作，适用于网站采集、数据同步、定时生成、通知和队列处理，不局限于爬虫。它们不授权修改未被用户指定的外部项目或服务器。

- 新建或修改相关 cron 时，同一工作流、运行环境和权限边界内的同类任务优先共用调度入口，不按每个任务或时点逐条增加 cron。延迟要求、数据依赖或故障隔离确需独立时保留多个入口，不以“整个服务器只能有一条 cron”为目标。
- 从项目现有脚本、文档及实际配置确认准确时刻、星期、时区和启用状态，在唯一权威配置中维护。共用 cron 取必要分钟、小时、星期的最小并集，应用负责精确分发；允许笛卡尔组合产生额外唤醒，但不默认每分钟轮询或全天每小时运行。低延迟队列确需高频检查时，记录依据。
- 明确 cron 与应用采用的时区、跨日星期、夏冬令时及必要提前量。不要把输出生成时间或允许工作窗口当作准确执行时刻；迟到启动按项目既有跳过或补做约定处理，不为合并而改变业务时刻、启停或重试语义。
- 先做必要的本地到期／队列检查，无工作即正常退出。除必要状态读取及队列锁外，空跑不读取凭据、不采集源数据、不发网络或模型请求。同刻到期且可合并的任务一起分发，共用来源只读一次；保留原有互斥、幂等、依赖顺序、日志归属和失败边界。成功、无任务及预期 SKIP 不发送失败通知，真实失败不得被吞掉。
- 安装前分别记录每周唤醒次数、命中工作的时点数、任务执行数及额外 SKIP 次数；同一时点可能有多个任务，命中后也可能因数据未变而无需更新。队列实际工作量由输入决定，不从唤醒频率推算。若笛卡尔空跑成本过高，应重新选择共用入口或少量独立 cron，不为减少行数增加大量唤醒。
- 从权威配置或生成器修改，按项目正常部署流程发布；备份现有 crontab，只替换自身管理块并移除被替代的旧条目，保留无关任务和环境设置，避免覆盖并发修改。验证目标时点完整覆盖、额外组合不触发外部工作，以及实际涉及的跨日、提前量、夏冬令时和同刻分发行为。
- 安装后回读实际 crontab，核对条目数量、表达式、时区、部署版本及无关内容未变，同步权威时刻表和部署记录。区分实现完成、安装完成与自然运行验收，不把局部验证当成全部完成。
