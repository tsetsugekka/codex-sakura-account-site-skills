# プロジェクト運用ルール

## cron の統合と実行管理

許可されたプロジェクトの収集・同期・通知・定期生成・キュー処理に適用する。

- 同じワークフローは共通の実行入口にまとめる。権限・遅延要件・依存関係・障害分離のために必要なら分け、行数削減のために空振り起動を増やさない。
- 実設定を確認し、実行時刻・曜日・タイムゾーン・有効状態を一つの正式な時刻表で管理する。cron は必要な分・時・曜日の最小集合の直積（デカルト積）で起動し、余分な組み合わせはアプリ側の正確な時刻判定で SKIP する。毎分・終日の起動を既定にせず、高頻度キューには遅延要件の根拠を持たせる。日付跨ぎ・夏時間・先行時間・遅延起動・再試行の扱いを維持し、業務スケジュールを勝手に変えない。
- 先に実行対象やキューを確認し、対象がなければ SKIP する。必要な状態読み取りとキューロックを除き、空振り時は認証情報の読み取り、収集、ネットワーク・モデル呼び出しを行わない。同時刻の関連タスクはまとめ、共通データは一度だけ読む。排他・冪等性・依存関係・ログを維持し、正常な SKIP は通知せず、実際の失敗は隠さない。
- 導入前に週あたりの起動回数・作業時点数・タスク実行数・追加 SKIP を区別して集計し、対象時刻の網羅と空振り時の外部呼び出しがないことを検証する。キューの実作業量は入力から集計し、起動回数から推定しない。
- 正式な設定または生成器を変更し、通常の手順で配備する。crontab をバックアップし、自分の管理ブロックだけ置換して旧項目を除き、並行変更を上書きしない。導入後に項目・タイムゾーン・配備版を読み戻し、無関係なジョブが変わっていないことを確認して文書を更新する。導入確認と定期実行の検証結果は分けて記録する。
