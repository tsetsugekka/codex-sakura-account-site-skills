# 项目维护规则

## Cron 合并与精确调度

适用于已授权项目中的采集、同步、通知、定时生成和队列任务。

- 同一工作流优先共用调度入口；权限、延迟、依赖或故障隔离确需独立时拆分，不为减少行数增加大量空跑。
- 核对实际配置，在唯一时刻表中维护执行时间、星期、时区和启停。cron 取必要分钟、小时、星期的最小并集，由应用精确分发；不默认每分钟或全天轮询，高频队列须有延迟依据。保留跨日、夏冬令时、提前量及迟到／重试约定，不擅改业务调度。
- 先检查到期任务或队列，无工作即 SKIP；除必要状态读取和队列锁外，不读取凭据或执行采集、网络、模型调用。同刻任务合理合并，共用来源只读一次，保留互斥、幂等、依赖和日志；正常跳过不告警，真实失败不得吞掉。
- 安装前区分并统计每周唤醒、工作时点、任务执行及额外 SKIP，验证目标覆盖和空跑无外部调用；队列实际工作量按输入统计，不从唤醒次数推算。
- 修改权威配置或生成器，按正常流程部署。备份 crontab，只替换自身管理块并移除旧条目，避免覆盖并发修改。安装后回读条目、时区和部署版本，确认无关任务未变，同步文档；分别记录安装与自然运行验收状态。
