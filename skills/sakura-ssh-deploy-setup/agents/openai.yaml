interface:
  display_name: "Sakura SSH Deploy Setup"
  short_description: "Prepare safe Sakura SSH and SFTP deploys"
  default_prompt: "Use $sakura-ssh-deploy-setup to create the local Sakura SSH secret through secure user input and prepare automated SSH/SFTP deployment."
